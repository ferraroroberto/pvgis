"""Streamlit views.  Each module exposes a ``render()`` callable.

Named ``views`` (not ``pages``) so Streamlit's built-in multipage
auto-discovery does *not* trigger.  Navigation is handled manually by
the sidebar in ``app.py``.
"""
